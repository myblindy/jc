program test; 

int main()
{
  if(a==b||a<b||a>b||a>=b||a<=b) then
    cout a;
  else
    cout b;
  fi

  // an array of numbers to check the validity of the number parser
  float[] arr=
    {
      10,
      10., 
      10.0,
      10.00,
      10.1, 
      10.10,
      0.0,   
      0.01, 
      0.10
    };


  for(int a:=1; a<10; a:=a+1)
    cout c;

  // next line has an error
  float f=1+3*4/3-2; 

  float f:= /* notice how its not =, but := */ 1+3*4/3-2;  

  return f;
}
